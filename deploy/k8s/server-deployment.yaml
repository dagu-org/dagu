apiVersion: apps/v1
kind: Deployment
metadata:
  name: boltbase-server
  namespace: boltbase-dev
  labels:
    app: boltbase
    component: server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: boltbase
      component: server
  template:
    metadata:
      labels:
        app: boltbase
        component: server
    spec:
      containers:
      - name: boltbase
        # Use official latest image
        image: ghcr.io/dagu-org/boltbase:latest
        imagePullPolicy: IfNotPresent
        command: ["boltbase", "start-all"]
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        - containerPort: 50055
          name: grpc
          protocol: TCP
        envFrom:
        - configMapRef:
            name: boltbase-config
        env:
        # Enable distributed mode: coordinator binds to all interfaces and advertises service name
        # Note: start-all only starts coordinator when host is not 127.0.0.1/localhost
        - name: BOLTBASE_COORDINATOR_HOST
          value: "0.0.0.0"
        - name: BOLTBASE_COORDINATOR_ADVERTISE
          value: "boltbase-server"
        volumeMounts:
        - name: boltbase-data
          mountPath: /var/lib/boltbase
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: boltbase-data
        persistentVolumeClaim:
          claimName: boltbase-data
---
apiVersion: v1
kind: Service
metadata:
  name: boltbase-server
  namespace: boltbase-dev
  labels:
    app: boltbase
    component: server
spec:
  type: NodePort
  selector:
    app: boltbase
    component: server
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    nodePort: 30080
    protocol: TCP
  - name: grpc
    port: 50055
    targetPort: 50055
    protocol: TCP
