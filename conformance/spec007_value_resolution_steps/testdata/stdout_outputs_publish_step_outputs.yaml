working_dir: .
steps:
  - id: build
    run: printf '{"image":"published-v1"}'
    stdout:
      outputs:
        fields:
          image:
            decode: json
            select: .image

  - id: deploy
    depends: build
    run: |
      printf '%s\n' '${steps.build.outputs.image}' > stdout-outputs.txt
