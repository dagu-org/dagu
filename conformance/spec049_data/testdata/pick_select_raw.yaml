working_dir: .
steps:
  - action: data.pick
    with:
      from: json
      data:
        user:
          name: alice
          age: 30
      select: ".user.name"
      raw: true
    stdout: result.out
