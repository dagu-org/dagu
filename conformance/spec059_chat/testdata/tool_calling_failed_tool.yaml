working_dir: .
env:
  - LLM_BASE_URL: ${LLM_BASE_URL}
steps:
  - action: chat.completion
    stdout: result.out
    with:
      prompt: "Run the broken tool."
      provider: local
      model: test-model
      base_url: $LLM_BASE_URL
      tools:
        - broken-tool
---
name: broken-tool
steps:
  - command: exit 1
