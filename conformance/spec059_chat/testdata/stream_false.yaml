working_dir: .
env:
  - LLM_BASE_URL: ${LLM_BASE_URL}
steps:
  - action: chat.completion
    stdout: result.out
    with:
      system: Be concise
      messages:
        - role: user
          content: hello
      provider: local
      model: test-model
      base_url: $LLM_BASE_URL
      stream: false
