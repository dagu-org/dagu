env:
  - PROJECT_DIR: $HOST_PROJECT_DIR
working_dir: $PROJECT_DIR
harnesses:
  fake_stdin:
    binary: ./scripts/fake_ok.sh
    prompt_mode: stdin
steps:
  - action: harness.run
    container:
      image: alpine:3
    with:
      provider: fake_stdin
      prompt: hello
