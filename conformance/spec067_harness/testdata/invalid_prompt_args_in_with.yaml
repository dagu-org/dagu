harnesses:
  fake_ok:
    binary: ./scripts/fake_ok.sh
    prompt_mode: arg
steps:
  - id: prompt_args_in_with
    action: harness.run
    with:
      provider: fake_ok
      prompt_args: ["--foo"]
      prompt: hi
