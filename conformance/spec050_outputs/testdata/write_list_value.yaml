working_dir: .
steps:
  - id: write
    action: outputs.write
    with:
      values:
        items:
          - a
          - b
  - id: read
    depends: write
    run: printf '%s' "${write.outputs.items}"
    stdout: result.out
