working_dir: .
steps:
  - name: pick
    action: router.route
    stdout: route.txt
    with:
      value: "ab"
      routes:
        a: [branch_a]
        ab: [branch_b]
  - name: branch_a
    run: echo ran-a > a.out
  - name: branch_b
    run: echo ran-b > b.out
