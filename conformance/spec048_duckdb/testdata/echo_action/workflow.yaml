type: graph

params:
  type: object
  additionalProperties: false
  required: [message]
  properties:
    message:
      type: string

steps:
  - id: echo
    run: printf '%s' "$message"
    stdout:
      outputs:
        field: echoed
