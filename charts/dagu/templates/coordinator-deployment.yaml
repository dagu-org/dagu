apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "boltbase.fullname" . }}-coordinator
  labels:
    {{- include "boltbase.labels" . | nindent 4 }}
    app.kubernetes.io/component: coordinator
spec:
  replicas: {{ .Values.coordinator.replicas }}
  selector:
    matchLabels:
      {{- include "boltbase.labels" . | nindent 6 }}
      app.kubernetes.io/component: coordinator
  template:
    metadata:
      labels:
        {{- include "boltbase.labels" . | nindent 8 }}
        app.kubernetes.io/component: coordinator
    spec:
      # Disable Kubernetes Service env var injection to avoid overriding
      # boltbase config values (e.g., scheduler.port) with Service URLs
      enableServiceLinks: false
      containers:
        - name: coordinator
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - boltbase
            - coordinator
            - --config
            - /etc/boltbase/boltbase.yaml
          env:
            - name: BOLTBASE_COORDINATOR_HOST
              value: "0.0.0.0"
            - name: BOLTBASE_COORDINATOR_ADVERTISE
              value: "{{ include "boltbase.fullname" . }}-coordinator.{{ .Release.Namespace }}.svc.cluster.local"
          ports:
            - name: grpc
              containerPort: 50055
              protocol: TCP
          volumeMounts:
            - name: data
              mountPath: /data
            - name: config
              mountPath: /etc/boltbase
          livenessProbe:
            tcpSocket:
              port: grpc
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: grpc
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.coordinator.resources | nindent 12 }}
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: {{ include "boltbase.fullname" . }}-data
        - name: config
          configMap:
            name: {{ include "boltbase.fullname" . }}-config
