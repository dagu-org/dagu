# goreleaser.yaml
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: boltbase
    dir: ./cmd
    binary: boltbase
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - freebsd
      - openbsd
      - windows
    goarch:
      - "amd64"
      - "386"
      - "arm"
      - "arm64"
      - "ppc64le"
      - "s390x"
    goarm:
      - "6"
      - "7"

release:
  mode: keep-existing
  prerelease: auto

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

brews:
  - skip_upload: auto
    repository:
      owner: dagu-org
      name: homebrew-brew
    directory: Formula
    homepage: "https://github.com/dagu-org/dagu"
    description: "A compact, portable, and language-agnostic workflow engine"
    license: "GNU General Public License v3.0"
    custom_block: |
      service do
        run [opt_bin/"boltbase", "start-all"]
        keep_alive true
        error_log_path var/"log/boltbase.log"
        log_path var/"log/boltbase.log"
        working_dir var
      end

  # Deprecated brew tap:
  - skip_upload: auto
    repository:
      owner: yohamta
      name: homebrew-tap
    directory: Formula
    homepage: "https://github.com/dagu-org/dagu"
    description: "A compact, portable, and language-agnostic workflow engine"
    license: "GNU General Public License v3.0"
    custom_block: |
      service do
        run [opt_bin/"boltbase", "start-all"]
        keep_alive true
        error_log_path var/"log/boltbase.log"
        log_path var/"log/boltbase.log"
        working_dir var
      end

